Every visible model call in this piece was actually two calls. Behind each Sonnet response the agent requested, a second, cheaper call to claude-haiku-4-5-20251001 fired automatically — roughly 900 input tokens spent naming the session, invisible in normal use, absent from any interface the agent could see at call time. It surfaced only when the raw usage JSON was inspected directly, outside the calibrated detector's three rules. This belongs in the statement, not a footnote, because it is not a bug or an aside: it is a fact about what "the agent" making this piece actually is — a system that does uninstructed, unbilled-to-attention work on its own behalf, whose complete metabolism its own operator cannot fully see without deliberately looking underneath it.