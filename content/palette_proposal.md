**Background:** `#1a1a1a` — near-black neutral; keeps data as the only saturated signal, works framed on white or black gallery walls alike.

**Baseline:** `#8a8f98` — desaturated grey-blue; recedes, reads as "nothing happened" in either context.

**Latency flag:** `#e8543e` — warm red-orange; urgent, unambiguous on dark or light crops.

**Retry flag:** `#f2c14e` — amber; distinct hue-family from red so first-occurrence flags aren't confused with slowness.

**Cache-deviation flag:** `#4ea8de` — cool cyan-blue; the "quiet" anomaly (statistical, not behavioral) gets a cooler, less alarming color, completing a balanced red/amber/blue triad against the grey baseline.